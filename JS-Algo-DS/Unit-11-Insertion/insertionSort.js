const insert = (arr) =>{
  for (i = 1; i < arr.length; i++){
    let currentVal = arr[i];
    for (let j = i -1; j >= 0; j--){
      if(arr[j] > currentVal){
        
      }
    }
  }
  return arr
}

console.log(insert(76, 2, 6, 1, 45, 22))